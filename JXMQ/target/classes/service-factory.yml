service-factory:
  
  scan-packages:
    - icu.nothingless.service
    - icu.nothingless.dao

  exclude-packages:
    # - icu.nothingless

  # lazy=首次使用时扫描，eager=启动时扫描
  scan-mode: eager
  
  # false 则需手动调用 scanPackage
  auto-scan: true